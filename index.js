function points(games) {
    let arr = games.split(':')
    games.forEach((item) =>{
        item.push(games[i])
    })
    return games;
}
let footballGame = ['1:0', '2:0', '4:5', '5:5']

points(footballGame)
